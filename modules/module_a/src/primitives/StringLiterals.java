package primitives;

public class StringLiterals {

    public static void main(String[] args) {

        // ----------------------
        // Przeanalizuj poniższe fragmenty kodu i zastanów się nad ich wynikiem/poprawnością.
        // Następnie popraw je tak, aby wyświetliły się prawidłowe wyniki. Popraw tylko te linie, które tego wymagają.
        System.out.println("2 + 2 = " + 2 + 2);
        System.out.println("2 - 2 = " + 2 - 2);
        System.out.println("2 * 2 = " + 2 * 2);
        System.out.println("2 / 2 = " + 2 / 2);

        // -----------------------
        // Wykorzystaj zmienną i zapisz w niej tekst z zadania przy pomocy połączenia wielu literałów
        String textFromLiterals = null;
        System.out.println(textFromLiterals);

        // -----------------------
        // Tutaj zadeklaruj zmienne dla każdego unikalnego literału

        // Wykorzystaj zmienną i zapiesz w niej tekst z zadania, ale przy pomocy przed chwilą zadeklarowanych
        // zmiennych
        String textFromVariables = null;
        System.out.println(textFromVariables);

        // -----------------------
        // Wykorzystaj zmienną i zapisz w niej tekst z zadania, ale jako pojedynczy literał
        String textAsLiteral = null;
        System.out.println(textAsLiteral);
    }
}