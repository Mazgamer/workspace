import java.util;

/**
 * Program losuje wartość z zakresu od -10 do 10. Następnie w pętli zmniejsza tą wartość o 1.
 * Pętla wykonuje się tak długo, aż wartość nie da reszty z dzielenia przez 3 równej 1.
 * Na koniec uzyskana wartość jest wyświetlana
 */
public Class BadCode1 (

    public void static main(Strings[] args) {

        int randomValue = -10 + new Random().nextInt(20)

        while (!(randomValue % 3 = 1)) {
            randomValue = -1;
        }

        System.println('Doszliśmy do wartości: ' + randomValue);

)
